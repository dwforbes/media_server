# Build output
/target/

# macOS
.DS_Store
._*

# rustfmt backups
**/*.rs.bk

# In case a catalog database ever gets pointed into the repo
*.db
*.db-shm
*.db-wal
*.uuid

# Live configs are per-machine; the repo tracks *.example.toml instead
/media-scanner.toml
/media-server.toml
